{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "qmake build",
            "type": "shell",
            "command": "cd ${workspaceFolder} && qmake && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}